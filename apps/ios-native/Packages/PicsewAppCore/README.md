# PicsewAppCore

App-facing native orchestration for the Picsew iOS product.

This package composes `PicsewMedia` and `PicsewAlgorithm` into one end-to-end processing pipeline that future SwiftUI features can call directly.
